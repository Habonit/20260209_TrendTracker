"""
# ============================================
# Phase 5-3: 레이아웃
# ============================================
#
# 🎯 이 파일에서 배울 것:
# - 컬럼으로 화면 나누기 (st.columns)
# - 사이드바 사용하기 (st.sidebar)
# - 접었다 펼 수 있는 영역 (st.expander)
# - 구분선과 컨테이너 (st.divider, st.container)
#
# 📋 실행 방법:
# streamlit run materials/phase_5_layout.py
# ============================================
"""

import streamlit as st

st.set_page_config(page_title="Phase 5-3: 레이아웃", page_icon="📐", layout="wide")

st.title("📐 Phase 5-3: 레이아웃")

st.markdown("""
## 레이아웃이란?

레이아웃은 **화면 배치**를 결정하는 것입니다.

신문을 생각해보세요:
- **여러 컬럼**으로 기사가 나뉘어 있고
- **사이드바**에 광고나 요약이 있고
- 클릭하면 **펼쳐지는** 상세 정보도 있죠

Streamlit도 이런 배치를 쉽게 만들 수 있습니다!
""")

st.divider()

# ============================================
# 📚 이론: 컬럼
# ============================================

st.header("1️⃣ 컬럼 (st.columns)")

st.markdown("""
### 컬럼이란?

화면을 **가로로 나누는** 것입니다.

```python
col1, col2 = st.columns(2)  # 2등분

with col1:
    st.write("왼쪽")

with col2:
    st.write("오른쪽")
```

### 비율 조정

```python
col1, col2, col3 = st.columns([2, 1, 1])  # 2:1:1 비율
```
""")

st.subheader("🔍 예제: 컬럼 나누기")

# 2등분 예제
st.markdown("**2등분 (1:1)**")
col1, col2 = st.columns(2)

with col1:
    st.info("👈 왼쪽 컬럼")
    st.write("여기는 왼쪽입니다")

with col2:
    st.info("👉 오른쪽 컬럼")
    st.write("여기는 오른쪽입니다")

# 비율 조정 예제
st.markdown("**비율 조정 (3:1:1)**")
col1, col2, col3 = st.columns([3, 1, 1])

with col1:
    st.success("넓은 영역 (3)")

with col2:
    st.warning("좁음 (1)")

with col3:
    st.error("좁음 (1)")

st.divider()

# ============================================
# 📚 이론: 사이드바
# ============================================

st.header("2️⃣ 사이드바 (st.sidebar)")

st.markdown("""
### 사이드바란?

화면 **왼쪽에 고정된 영역**입니다.
설정, 필터, 네비게이션 등을 배치합니다.

```python
st.sidebar.title("사이드바 제목")
st.sidebar.slider("값 선택", 1, 10)
```

### 사이드바 vs 메인 영역

| 사이드바 | 메인 영역 |
|---------|----------|
| `st.sidebar.xxx()` | `st.xxx()` |
| 항상 왼쪽에 고정 | 스크롤 가능 |
| 설정, 필터 용도 | 메인 콘텐츠 |

👈 **왼쪽 사이드바를 확인해보세요!**
""")

# 사이드바 구성
st.sidebar.title("🎓 학습 사이드바")
st.sidebar.markdown("이 영역이 사이드바입니다!")

sidebar_name = st.sidebar.text_input("이름 입력", key="sidebar_name")
sidebar_age = st.sidebar.slider("나이 선택", 1, 100, 25, key="sidebar_age")

st.sidebar.divider()

if sidebar_name:
    st.sidebar.success(f"{sidebar_name}님 ({sidebar_age}살)")

st.divider()

# ============================================
# 📚 이론: Expander
# ============================================

st.header("3️⃣ 접었다 펼치기 (st.expander)")

st.markdown("""
### Expander란?

**클릭하면 펼쳐지는** 영역입니다.
긴 내용을 숨겨두고 필요할 때만 보여줄 수 있어요!

```python
with st.expander("클릭하면 펼쳐집니다"):
    st.write("숨겨진 내용")
```

### 파라미터

| 파라미터 | 설명 |
|---------|------|
| `expanded=False` | 처음에 접힌 상태 (기본) |
| `expanded=True` | 처음에 펼쳐진 상태 |
""")

st.subheader("🔍 예제: Expander")

with st.expander("📖 사용법 보기 (접힌 상태)", expanded=False):
    st.markdown("""
    1. 키워드를 입력합니다
    2. 검색 버튼을 클릭합니다
    3. 결과를 확인합니다
    """)

with st.expander("⚠️ 주의사항 (펼쳐진 상태)", expanded=True):
    st.warning("이 영역은 처음부터 펼쳐져 있습니다!")
    st.write("중요한 정보는 `expanded=True`로 보여주세요.")

st.divider()

# ============================================
# 📚 이론: 기타 레이아웃
# ============================================

st.header("4️⃣ 기타 레이아웃")

st.markdown("""
### 구분선 (st.divider)

섹션을 구분하는 **가로선**입니다.

```python
st.write("위")
st.divider()
st.write("아래")
```

### 컨테이너 (st.container)

요소들을 **그룹으로 묶는** 영역입니다.
나중에 특정 위치에 요소를 추가할 때 유용!

```python
container = st.container()
container.write("나중에 추가")
```
""")

st.subheader("🔍 예제: 컨테이너로 순서 바꾸기")

# 컨테이너로 순서 제어
container = st.container()

st.write("3️⃣ 이 코드가 먼저 있지만...")

container.write("1️⃣ 컨테이너 안에 먼저 표시됩니다!")
container.write("2️⃣ 컨테이너 내용이 위에 나와요")

st.divider()

# ============================================
# ✏️ 가이드 실습
# ============================================

st.header("✏️ 가이드 실습: 대시보드 레이아웃")

st.markdown("""
### 목표
컬럼과 사이드바를 활용하여 간단한 대시보드 만들기

### 힌트
- 사이드바: 설정 (이름, 슬라이더)
- 메인: 2컬럼으로 정보 표시
- Expander로 상세 정보 숨기기
""")

st.subheader("🎯 실습 영역")

# ============================================
# TODO 1: 사이드바에 "설정" 제목과 숫자 슬라이더 추가
# 슬라이더: "표시할 항목 수", 1~10, 기본값 5
# ============================================

# >>> 정답 <<<
# st.sidebar.subheader("⚙️ 설정")
# num_items = st.sidebar.slider("표시할 항목 수", 1, 10, 5, key="practice_items")

# 정답 실행 (사이드바에 추가)
st.sidebar.divider()
st.sidebar.subheader("⚙️ 실습: 설정")
num_items = st.sidebar.slider("표시할 항목 수", 1, 10, 5, key="practice_items")

# ============================================
# TODO 2: 메인 영역을 2컬럼으로 나누기
# 왼쪽: st.metric()으로 "총 항목" 표시
# 오른쪽: st.metric()으로 "페이지" 표시
# ============================================

# >>> 정답 <<<
# left, right = st.columns(2)
# with left:
#     st.metric("총 항목", num_items)
# with right:
#     st.metric("페이지", 1)

# 정답 실행
left, right = st.columns(2)
with left:
    st.metric("총 항목", num_items)
with right:
    st.metric("페이지", 1)

# ============================================
# TODO 3: st.expander()로 "상세 정보" 영역 만들기
# 내용: 선택한 항목 수에 따라 리스트 표시
# ============================================

# >>> 정답 <<<
# with st.expander("📋 상세 정보"):
#     for i in range(num_items):
#         st.write(f"- 항목 {i+1}")

# 정답 실행
with st.expander("📋 상세 정보"):
    for i in range(num_items):
        st.write(f"- 항목 {i+1}")

st.markdown("""
### 💡 실습 해설

**TODO 1**: `st.sidebar.xxx()`로 사이드바에 요소를 추가합니다.

**TODO 2**: `st.metric()`은 숫자를 강조해서 보여주는 컴포넌트입니다. 대시보드에서 많이 씁니다!

**TODO 3**: `st.expander()` 안에서 `for` 문을 사용해 동적으로 내용을 표시할 수 있습니다.
""")

st.divider()

# ============================================
# 🏆 챌린지
# ============================================

st.header("🏆 챌린지: 뉴스 카드 레이아웃")

st.markdown("""
### 목표
뉴스 기사 목록을 카드 형태로 보여주는 레이아웃 만들기

⚠️ **정답이 제공되지 않습니다.** 강사와 함께 풀어보세요!
""")

st.subheader("🎯 챌린지 영역")

st.code('''
# ============================================
# 🏆 챌린지: 뉴스 카드 레이아웃
# ============================================
# ⚠️ 정답이 제공되지 않습니다.

# 샘플 데이터
news_list = [
    {"title": "AI 기술 발전", "summary": "최신 AI 기술이 발표되었습니다."},
    {"title": "경제 전망", "summary": "올해 경제 성장률 전망입니다."},
    {"title": "기후 변화", "summary": "지구 온난화 대응 방안입니다."},
    {"title": "스포츠 소식", "summary": "어제 경기 결과입니다."},
]

# TODO 1: 사이드바에 "카테고리" 셀렉트박스 추가
# 옵션: ["전체", "기술", "경제", "환경", "스포츠"]


# TODO 2: 뉴스를 2컬럼으로 표시 (2개씩 한 줄에)
# 힌트: for 문과 columns를 조합
# for i in range(0, len(news_list), 2):  # 2개씩
#     col1, col2 = st.columns(2)
#     ...


# TODO 3: 각 뉴스를 expander로 표시
# 제목은 expander 레이블, 요약은 내용


# TODO 4: (심화) 사이드바에서 "컬럼 수" 선택 가능하게
# 1컬럼, 2컬럼, 3컬럼 중 선택

''', language="python")

st.divider()

# ============================================
# 📋 정리
# ============================================

st.header("📋 정리")

st.markdown("""
### 이번 파일에서 배운 것

| 컴포넌트 | 용도 |
|---------|------|
| `st.columns()` | 화면을 가로로 나눔 |
| `st.sidebar` | 왼쪽 고정 영역 |
| `st.expander()` | 접었다 펼 수 있는 영역 |
| `st.divider()` | 구분선 |
| `st.container()` | 요소 그룹화 |

### 핵심 패턴

```python
# 컬럼 나누기
col1, col2 = st.columns(2)
with col1:
    st.write("왼쪽")
with col2:
    st.write("오른쪽")

# 사이드바
st.sidebar.title("사이드바")

# Expander
with st.expander("제목"):
    st.write("숨겨진 내용")
```

### ✅ 체크리스트
- [ ] st.columns()로 화면을 나눌 수 있다
- [ ] st.sidebar로 사이드바를 구성할 수 있다
- [ ] st.expander()로 접었다 펼 수 있는 영역을 만들 수 있다
- [ ] 컬럼 비율을 조정할 수 있다 (예: [3, 1, 1])
- [ ] 챌린지를 강사와 함께 풀어봤다
""")

st.info("다음 파일: `phase_5_state.py` - 상태 관리 (session_state)")
